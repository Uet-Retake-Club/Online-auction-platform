<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.auction.client.controllers.MainLayoutController"
            fx:id="mainRoot"
            maxWidth="Infinity" maxHeight="Infinity">

    <!-- ════════════════════════════════════════════════════
         GLOBAL TOP NAVIGATION BAR
         ════════════════════════════════════════════════════ -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" styleClass="top-nav">
            <!-- Logo -->
            <HBox alignment="CENTER_LEFT" spacing="0" onMouseClicked="#onHome" style="-fx-cursor: hand;">
                <Label text="Auction" styleClass="heading-3" style="-fx-text-fill: -primary; -fx-font-size: 22px;"/>
                <Label text="Hub"     styleClass="heading-3" style="-fx-text-fill: -text-main; -fx-font-size: 22px;"/>
            </HBox>

            <Region HBox.hgrow="ALWAYS"/>

            <!-- Nav Actions -->
            <HBox alignment="CENTER" spacing="8">
                <Button fx:id="watchlistBtn" text="Watchlist" onAction="#onWatchlist" styleClass="btn-ghost"/>
                <Button fx:id="myBidsBtn"    text="My bids"   onAction="#onMyBids"    styleClass="btn-ghost"/>
                <Button fx:id="adminBtn"     text="🛡 Admin"  onAction="#onAdmin"    visible="false" managed="false" styleClass="btn-primary" style="-fx-background-color: -info;"/>
                <Button fx:id="sellBtn"      text="+ Sell"    onAction="#onSell"     styleClass="btn-primary"/>
                
                <Separator orientation="VERTICAL" style="-fx-padding: 0 10px;"/>
                
                <Button text="☀/🌙" onAction="#onToggleTheme" styleClass="btn-ghost" style="-fx-font-size: 16px;"/>
                
                <HBox alignment="CENTER" spacing="8" styleClass="badge" style="-fx-background-color: -primary-light; -fx-padding: 8px 16px; -fx-min-width: 140px; -fx-max-width: 220px; -fx-cursor: hand;" onMouseClicked="#onWallet">
                    <Label text="💳" style="-fx-font-size: 14px;"/>
                    <Label fx:id="navWalletBalanceLabel" text="Loading..." styleClass="label-bold" style="-fx-text-fill: -primary;"/>
                </HBox>

                <Label fx:id="userLabel"
                       text="JD"
                       onMouseClicked="#onProfile"
                       styleClass="badge"
                       style="-fx-background-color: -primary-light; -fx-text-fill: -primary; -fx-padding: 8px 14px; -fx-background-radius: 20px; -fx-cursor: hand;"/>
            </HBox>
        </HBox>
    </top>

    <!-- ════════════════════════════════════════════════════
         DYNAMIC CONTENT AREA
         ════════════════════════════════════════════════════ -->
    <center>
        <StackPane fx:id="contentArea"/>
    </center>

</BorderPane>
